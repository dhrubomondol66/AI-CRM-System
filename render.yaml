services:
  - type: web
    name: ai-reservation-crm-system
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    pullRequestPreviewsEnabled: false
    envVars:
      - key: VITE_API_BASE_URL
        value: https://reservation-api-kuzr.onrender.com